#include "Parser.hpp"

#include <fstream>
#include <iostream>
#include <vector>

struct Tag {};

int main() {
    Parser<Tag> parser;
    // parser.setNamedValue<Parser<std::vector<int> > >(std::string("error_code"));
    // parser.setNamedValue<Parser<std::string> >(std::string("error_page"));

}